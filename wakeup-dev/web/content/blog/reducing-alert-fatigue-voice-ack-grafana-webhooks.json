{
  "slug": "reducing-alert-fatigue-voice-ack-grafana-webhooks",
  "title": "Reducing Alert Fatigue with Voice ACKs and Grafana Webhooks",
  "description": "Grafana POSTs a raw body to /v1/alert with x-api-key. WakeUp Dev owns the voice cascade and digit-1 ACK internally—no Express, no ACK routes, unlimited seats.",
  "keywords": [
    "alert-fatigue",
    "voice-ack",
    "grafana",
    "on-call",
    "devops"
  ],
  "coverImage": "https://source.unsplash.com/1600x900/?voice-alert,grafana-dashboard,cloudflare-workers",
  "dateIso": "2026-09-01",
  "dateLabel": "September 1, 2026",
  "author": "Enrique Drack",
  "topic": "Cutting Alert Fatigue with Voice ACKs and Grafana Webhooks",
  "blocks": [
    {
      "type": "h2",
      "text": "Overview"
    },
    {
      "type": "p",
      "text": "WakeUp Dev ingests alerts from Grafana, UptimeRobot, or any HTTP client with a single public call: a raw POST to https://api.wakeupdev.com/v1/alert plus your x-api-key. After that, the platform owns the rest of the path—voice cascade, digit-1 ACK, and stopping escalation. You do not stand up Express, ACK routes, or TwiML servers."
    },
    {
      "type": "p",
      "text": "The ACK that cuts alert fatigue is a human pressing 1 on the phone. Pickup is not enough. That IVR loop runs privately between Cloudflare Workers and Twilio."
    },
    {
      "type": "h2",
      "text": "Grafana webhook integration"
    },
    {
      "type": "p",
      "text": "Point Grafana (or any monitor) at the ingest URL. Send the payload as the raw request body. WakeUp Dev reads that body as the alert text for the call—no required title / message / severity schema on your side."
    },
    {
      "type": "code",
      "code": "curl -X POST https://api.wakeupdev.com/v1/alert \\\n  -H \"x-api-key: YOUR_API_KEY\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"alerts\":[{\"status\":\"firing\",\"labels\":{\"alertname\":\"HighCPU\"},\"annotations\":{\"summary\":\"prod CPU above 90%\"}}]}'"
    },
    {
      "type": "p",
      "text": "That is the only integration surface. If the key is valid and you have credits, WakeUp Dev starts the on-call cascade."
    },
    {
      "type": "h2",
      "text": "Voice ACK (handled for you)"
    },
    {
      "type": "p",
      "text": "You never implement /v1/ack. Digit capture is Worker-to-Twilio on a private callback (/v1/twilio/ivr-callback). The TwiML below is conceptual: Gather posts back into WakeUp Dev, not into your app. The action host is the product site; the Worker completes ACK and stops the cascade without a webhook back to you."
    },
    {
      "type": "code",
      "code": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<Response>\n  <Say>Alert: CPU usage is above ninety percent. Press one to acknowledge.</Say>\n  <Gather numDigits=\"1\" action=\"https://wakeupdev.com\" method=\"POST\">\n    <Say>Press one to acknowledge.</Say>\n  </Gather>\n</Response>"
    },
    {
      "type": "p",
      "text": "If nobody presses 1, WakeUp Dev escalates to the next on-call target. You do not poll, retry, or host IVR."
    },
    {
      "type": "h2",
      "text": "Unlimited seats and cost"
    },
    {
      "type": "p",
      "text": "WakeUp Dev is pay-per-voice-alert, not per seat. Add responders in the dashboard; the bill does not grow with headcount. Digit-1 ACK keeps calls short."
    },
    {
      "type": "h2",
      "text": "Example: send an alert from Node.js"
    },
    {
      "type": "p",
      "text": "This is the full client. There is no ACK handler to add."
    },
    {
      "type": "code",
      "code": "const API_KEY = process.env.WAKEUP_API_KEY;\n\nasync function sendAlert(payload) {\n  const response = await fetch(\"https://api.wakeupdev.com/v1/alert\", {\n    method: \"POST\",\n    headers: {\n      \"x-api-key\": API_KEY,\n      \"Content-Type\": \"application/json\",\n    },\n    body: JSON.stringify(payload),\n  });\n\n  if (!response.ok) {\n    throw new Error(`WakeUp Dev alert failed: ${response.status} ${await response.text()}`);\n  }\n\n  return response.json();\n}\n\nawait sendAlert({\n  source: \"grafana\",\n  summary: \"Disk usage on /dev/sda1 is 95% on prod.\",\n});"
    },
    {
      "type": "p",
      "text": "Grafana can POST the same way from a contact point: URL https://api.wakeupdev.com/v1/alert, header x-api-key."
    },
    {
      "type": "h2",
      "text": "Best practices"
    },
    {
      "type": "ul",
      "items": [
        "Keep the Grafana body short; it is spoken on the call.",
        "Treat x-api-key like a secret; Grafana contact points support a custom header.",
        "Do not build ACK or TwiML endpoints in your stack—WakeUp Dev already runs that loop.",
        "Use the dashboard cascade for routing, not extra Slack/email pages for the same firing alert."
      ]
    },
    {
      "type": "discussion",
      "text": "What strategies have you used to reduce alert fatigue when one webhook should page a human by phone, without adding another per-seat tool or a homemade IVR?"
    }
  ]
}
